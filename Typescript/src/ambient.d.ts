/**
 * Its a file outside my code with no execution 
 * which provides the types for the execution file.
 */

declare namespace MyLibrary {
    function myFunction(x: number, y: number): number;
    const myString: string;
  }